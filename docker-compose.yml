version: '3.8'

services:
  teamviewer-relay:
    build: .
    container_name: teamviewer-relay
    ports:
      - "5900:5900"
    restart: unless-stopped
    networks:
      - teamviewer-net

networks:
  teamviewer-net:
    driver: bridge
